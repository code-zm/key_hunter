# ============================================================================
# GitHub Personal Access Token for searching repositories
# ============================================================================
# Create at: https://github.com/settings/tokens
# Required scopes: public_repo (read-only access to public repositories)
#
# ⚠️  IMPORTANT: Do not create multiple Personal Accounts to bypass rate limits.
# This violates GitHub's Terms of Service (Section B.3) and may result in
# account suspension or termination.
#
# Rate limits: ~30 requests/minute per token (authenticated)
GITHUB_TOKEN1=ghp_your_token_here

# ============================================================================
# SMTP Configuration for Email Notifications
# ============================================================================
# The report command sends email notifications to repository/commit authors
# when valid API keys are discovered.
#
# SMTP Server Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Sender Information
SMTP_FROM_EMAIL=security@yourdomain.com
SMTP_FROM_NAME=Key Hunter Security Alert

# ⚠️  GMAIL USERS: Use an App Password, not your account password
# Create at: https://myaccount.google.com/apppasswords
#
# ⚠️  OTHER PROVIDERS:
# - Outlook/Office365: smtp.office365.com:587
# - Yahoo: smtp.mail.yahoo.com:587
# - SendGrid: smtp.sendgrid.net:587
# - Mailgun: smtp.mailgun.org:587
#
# ⚠️  ETHICAL USE ONLY: Only send notifications for security purposes
# with the intent to help repository owners secure their credentials.

# GitLab Personal Access Token (optional)
# GITLAB_TOKEN=glpat_your_token_here
