# GitHub Personal Access Token for searching repositories
# Create at: https://github.com/settings/tokens
# Required scopes: public_repo (read-only access to public repositories)
#
# ⚠️  IMPORTANT: Do not create multiple Personal Accounts to bypass rate limits.
# This violates GitHub's Terms of Service (Section B.3) and may result in
# account suspension or termination.
#
# Rate limits: ~30 requests/minute per token (authenticated)
GITHUB_TOKEN1=ghp_your_token_here

# GitHub Personal Access Token for creating issues
# Create at: https://github.com/settings/tokens
# Required scopes: public_repo (allows creating issues on public repositories)
#
# ⚠️  CRITICAL WARNING: Only use the 'report' command on repositories you own
# or have explicit permission to modify. Creating issues on repositories you
# don't own may violate GitHub's Terms of Service and result in account suspension.
#
# The 'report' command should ONLY be used for:
# - Your own repositories
# - Repositories where you're an authorized collaborator
# - Internal organization security audits (with permission)
ISSUES_GITHUB_TOKEN=ghp_your_token_here

# GitLab Personal Access Token (optional)
# GITLAB_TOKEN=glpat_your_token_here
